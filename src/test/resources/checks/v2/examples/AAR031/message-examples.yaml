asyncapi: "2.0.0"
info:
  title: Bad API
  version: "1.0.0"
channels:
  /pets:
    subscribe:
      operationId: getPets
      message: # Noncompliant {{AAR031: All examples in message object should follow payload and headers schemas}}
        contentType: application/json
        payload:
          type: object
          properties:
            id:
              type: integer
            name:
              type: string
