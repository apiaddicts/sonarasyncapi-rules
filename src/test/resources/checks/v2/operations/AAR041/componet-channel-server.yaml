asyncapi: '2.4.0'
info:
  title: Component Channel Server
  version: '1.0.0'
servers:
  production:
    url: 'api.example.com'
    protocol: 'wss'
channels:
  events:
    description: Event channel
    subscribe:
      operationId: receiveEvent
      summary: Receive events
      message:
        contentType: application/json
        payload:
          type: object
