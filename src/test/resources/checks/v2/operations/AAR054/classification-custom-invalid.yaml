asyncapi: '2.6.0'
info:
  title: Classification Custom Invalid V2
  version: '1.0.0'
channels:
  beca.cmd.alumnos.registro-bonificacion.beca: # Noncompliant {{AAR054: Channel name's classification segment (2nd segment) must be one of: cdc, cmd, sys}}
    subscribe:
      operationId: receiveRegistration
      message:
        payload:
          type: object
