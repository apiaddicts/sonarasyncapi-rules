asyncapi: '2.4.0'
info:
  title: My Async API
  version: '1.0.0'
servers:
  emptyObj:
    url: o:9092
    protocol: {} # Noncompliant {{AAR064: The server protocol must be 'kafka' or 'kafka-ssl'}}
  emptyArr:
    url: a:9092
    protocol: [] # Noncompliant {{AAR064: The server protocol must be 'kafka' or 'kafka-ssl'}}
channels:
  user-events: {}
