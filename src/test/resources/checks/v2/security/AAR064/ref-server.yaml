asyncapi: '2.4.0'
info:
  title: My Async API
  version: '1.0.0'
servers:
  production:
    $ref: '#/components/servers/insecure'
channels:
  user-events: {}
components:
  servers:
    insecure:
      url: broker.example.com:9092
      protocol: https
