asyncapi: '2.4.0'
info:
  title: My Async API
  version: '1.0.0'
servers:
  production:
    $ref: '#/components/servers/secure'
channels:
  user-events: {}
components:
  servers:
    secure:
      url: broker.example.com:9092
      protocol: kafka-ssl
