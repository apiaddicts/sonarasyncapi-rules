asyncapi: '2.4.0'
info:
  title: Security Channel Example
  version: '1.0.0'
channels:
  /pets: # Noncompliant {{AAR043: Channel should define a security scheme in its operations or bindings.}}
    subscribe:
      operationId: getPets
      message:
        contentType: application/json
        payload:
          type: object
