asyncapi: '2.4.0'
info:
  title: Swagger Petstore
  version: '1.0.0'
servers:
  default:
    url: 'http://localhost:8080'
    protocol: 'http' # Noncompliant {{AAR001: Server must use a secure protocol (https, wss, amqps, etc).}}
    description: Servidor por defecto para la tienda de mascotas
channels:
  /pets: {}
