asyncapi: '2.4.0'
info:
  title: Avro Record Doc Invalid V2
  version: '1.0.0'
channels:
  orders:
    subscribe:
      operationId: receiveOrder
      message:
        schemaFormat: 'application/vnd.apache.avro;version=1.9.0'
        payload: # Noncompliant {{AAR046: Avro record should include the doc field with a description}}
          type: record
          name: OrderValue
          namespace: org.example.orders
          fields:
            - name: id
              type: string
