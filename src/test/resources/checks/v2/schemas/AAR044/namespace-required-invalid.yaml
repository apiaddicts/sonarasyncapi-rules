asyncapi: '2.4.0'
info:
  title: Avro Namespace Required Invalid V2
  version: '1.0.0'
channels:
  orders:
    subscribe:
      operationId: receiveOrder
      message:
        schemaFormat: 'application/vnd.apache.avro;version=1.9.0'
        payload: # Noncompliant {{AAR044: Avro record must have the namespace field defined}}
          type: record
          name: OrderValue
          fields:
            - name: id
              type: string
