asyncapi: '2.4.0'
info:
  title: Null Content Type
  version: '1.0.0'
servers:
  production:
    url: 'api.example.com'
    protocol: 'wss'
channels:
  orders:
    subscribe:
      operationId: receiveOrder
      message: # Noncompliant {{AAR024: Each message must declare a 'contentType' unless it is an Avro message}}
        contentType: null
        payload:
          type: object
