asyncapi: '2.4.0'
info:
  title: ID Schemas
  version: '1.0.0'
servers:
  production:
    url: 'api.example.com'
    protocol: 'wss'
channels:
  users:
    subscribe:
      operationId: receiveUser
      message:
        contentType: application/json
        payload:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
