# Noncompliant {{AAR008: The AsyncAPI document must define at least one server in the 'servers' object.}}
asyncapi: 3.0.0
info:
  title: Swagger Petstore
  version: '1.0.0'
channels:
  user-signup:
    address: user/signup
    messages:
      UserSignedUp:
        payload:
          type: object
