asyncapi: '3.0.0'
info:
  title: My Async API
  version: '1.0.0'
servers:
  nullServer: null
  scalarServer: just-a-string
  missingProtocol:
    host: m:9092
    description: No protocol field at all
  nullProtocol:
    host: n:9092
    protocol: null
  validServer:
    host: v:9092
    protocol: kafka-ssl
channels:
  userEvents:
    address: user-events
    messages:
      Event:
        payload: {type: object}
