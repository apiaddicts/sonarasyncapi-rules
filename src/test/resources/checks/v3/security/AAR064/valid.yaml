asyncapi: '3.0.0'
info:
  title: My Async API
  version: '1.0.0'
servers:
  production:
    host: broker.example.com:9092
    protocol: kafka-ssl
    description: Production Kafka broker
channels:
  userEvents:
    address: user-events
    messages:
      Event:
        payload: {type: object}
