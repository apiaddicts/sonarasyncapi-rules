asyncapi: '3.0.0'
info:
  title: My Async API
  version: '1.0.0'
servers:
  production:
    $ref: '#/components/servers/secure'
channels:
  userEvents:
    address: user-events
    messages:
      Event:
        payload: {type: object}
components:
  servers:
    secure:
      host: broker.example.com:9092
      protocol: kafka
