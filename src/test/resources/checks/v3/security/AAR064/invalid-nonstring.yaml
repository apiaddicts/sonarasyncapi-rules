asyncapi: '3.0.0'
info:
  title: My Async API
  version: '1.0.0'
servers:
  numeric:
    host: n:9092
    protocol: 123 # Noncompliant {{AAR064: The server protocol must be 'kafka' or 'kafka-ssl'}}
  boolean:
    host: b:9092
    protocol: true # Noncompliant {{AAR064: The server protocol must be 'kafka' or 'kafka-ssl'}}
channels:
  userEvents:
    address: user-events
    messages:
      Event:
        payload: {type: object}
