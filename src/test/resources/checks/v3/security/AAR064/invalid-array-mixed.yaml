asyncapi: '3.0.0'
info:
  title: My Async API
  version: '1.0.0'
servers:
  - host: g:9092
    protocol: kafka
  - host: b:9092
    protocol: https # Noncompliant {{AAR064: The server protocol must be 'kafka' or 'kafka-ssl'}}
  - host: g2:9092
    protocol: kafka-ssl
channels:
  userEvents:
    address: user-events
    messages:
      Event:
        payload: {type: object}
