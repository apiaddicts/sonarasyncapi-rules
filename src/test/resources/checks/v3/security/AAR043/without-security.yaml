# Noncompliant {{AAR043: Channel should define a security scheme in its operations or bindings.}}
asyncapi: 3.0.0
info:
  title: No Security
  version: 1.0.0
servers:
  - host: api.example.com
    protocol: wss
channels:
  events:
    address: events
    messages:
      Event:
        payload: {type: object}
