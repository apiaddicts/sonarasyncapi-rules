# Noncompliant {{AAR001: Server must use a secure protocol (https, wss, amqps, etc).}}
asyncapi: 3.0.0
info:
  title: MQTT Insecure
  version: 1.0.0
servers:
  - host: mqtt.example.com
    protocol: mqtt
    port: 1883
channels:
  data:
    address: data
    messages:
      Data:
        payload: {type: object}
