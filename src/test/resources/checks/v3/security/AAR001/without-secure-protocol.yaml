# Noncompliant {{AAR001: Server must use a secure protocol (https, wss, amqps, etc).}}
asyncapi: 3.0.0
info:
  title: Insecure API
  version: 1.0.0
servers:
  - host: api.example.com
    protocol: ws
    pathname: /api
channels:
  events:
    address: events
    messages:
      Event:
        payload: {type: object}
