asyncapi: 3.0.0
info:
  title: Invalid Server Reference
  version: 1.0.0
servers:
  production:
    host: api.example.com
    protocol: wss
channels:
  notifications:
    address: notifications
    servers:
      - unknown # Noncompliant {{AAR040: Channel references a server that is not defined in the 'servers' object.}}
    messages:
      Notification:
        payload: {type: object}
operations:
  SendNotification:
    action: send
    channel: notifications
