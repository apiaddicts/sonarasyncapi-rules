asyncapi: 3.0.0
info:
  title: Classification Uppercase Segment V3
  version: 1.0.0
channels:
  scholarshipRegistration:
    address: beca.Cmd.alumnos.registro-bonificacion.beca # Noncompliant {{AAR054: Channel name's classification segment (2nd segment) must be one of: cdc, cmd, sys}}
    messages:
      Registration:
        payload: {type: object}
