asyncapi: 3.0.0
info:
  title: Classification Invalid Unquoted Boolean Address V3
  version: 1.0.0
channels:
  scholarshipRegistration:
    address: true # Noncompliant {{AAR054: Channel name's classification segment (2nd segment) must be one of: cdc, cmd, sys}}
    messages:
      Registration:
        payload: {type: object}
