asyncapi: 3.0.0
info:
  title: Processor Function Name Paired - Structural Edges V3
  version: 1.0.0
channels:
  a-in:
    address: a.in
operations:
  null-operation: null
  scalar-operation: just-a-string
  no-action:
    channel:
      $ref: '#/channels/a-in'
    x-scs-function-name: orphanNoAction
  other-action:
    action: publish
    channel:
      $ref: '#/channels/a-in'
    x-scs-function-name: orphanOtherAction
  null-function:
    action: receive
    channel:
      $ref: '#/channels/a-in'
    x-scs-function-name: null
