asyncapi: 3.0.0
info:
  title: Processor Function Name Paired - Blank Value V3
  version: 1.0.0
channels:
  a-in:
    address: a.in
operations:
  emptyString:
    action: send
    channel:
      $ref: '#/channels/a-in'
    x-scs-function-name: ""
  whitespaceOnly:
    action: receive
    channel:
      $ref: '#/channels/a-in'
    x-scs-function-name: "   "
