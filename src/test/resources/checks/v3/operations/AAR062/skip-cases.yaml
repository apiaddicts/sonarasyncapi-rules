asyncapi: 3.0.0
info:
  title: Subscribe Group Required - Skip Cases V3
  version: 1.0.0
channels:
  alta-beca:
    address: alta.beca
operations:
  refOperation:
    $ref: '#/components/operations/sharedConsumer'
  brokenOperation: null
  noAction:
    channel:
      $ref: '#/channels/alta-beca'
  produceSalida:
    action: send
    channel:
      $ref: '#/channels/alta-beca'
  # Consuming operation with a scalar kafka groupId: valid.
  consumeScalar:
    action: receive
    channel:
      $ref: '#/channels/alta-beca'
    bindings:
      kafka:
        groupId: alta.beca.1
components:
  operations:
    sharedConsumer:
      action: receive
      channel:
        $ref: '#/channels/alta-beca'
      x-scs-group: alta.beca.1
