asyncapi: 3.0.0
info:
  title: Subscribe Group Required - $ref to component operation without group V3
  version: 1.0.0
channels:
  alta-beca:
    address: alta.beca
operations:
  refOperationNoGroup:
    $ref: '#/components/operations/sharedConsumerNoGroup'
components:
  operations:
    sharedConsumerNoGroup: # Noncompliant {{AAR062: Each consuming operation must declare a consumer group via 'x-scs-group' or 'bindings.kafka.groupId'}}
      action: receive
      channel:
        $ref: '#/channels/alta-beca'
