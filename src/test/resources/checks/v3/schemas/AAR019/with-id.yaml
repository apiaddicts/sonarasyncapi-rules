asyncapi: 3.0.0
info:
  title: Schema with ID
  version: 1.0.0
servers:
  - host: api.example.com
    protocol: wss
channels:
  users:
    address: users
    messages:
      User:
        payload:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
