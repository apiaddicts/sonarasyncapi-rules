asyncapi: 3.0.0
info:
  title: Avro Namespace Special Chars V3
  version: 1.0.0
channels:
  orders:
    address: orders
    messages:
      OrderSpace:
        payload:
          schemaFormat: 'application/vnd.apache.avro;version=1.9.0'
          schema: # Noncompliant {{AAR052: Avro namespace must follow the corporate pattern: org.madrid.<cod_poaps>.<classification>.<domain> for application schemas, or org.madrid.common.<domain> for common schemas}}
            type: record
            name: OrderValue
            namespace: 'org.madrid.p 001.core.orders'
            fields:
              - name: id
                type: string
      OrderDoubleDot:
        payload:
          schemaFormat: 'application/vnd.apache.avro;version=1.9.0'
          schema: # Noncompliant {{AAR052: Avro namespace must follow the corporate pattern: org.madrid.<cod_poaps>.<classification>.<domain> for application schemas, or org.madrid.common.<domain> for common schemas}}
            type: record
            name: OrderValue
            namespace: org.madrid.p001..orders
            fields:
              - name: id
                type: string
      OrderUnicode:
        payload:
          schemaFormat: 'application/vnd.apache.avro;version=1.9.0'
          schema: # Noncompliant {{AAR052: Avro namespace must follow the corporate pattern: org.madrid.<cod_poaps>.<classification>.<domain> for application schemas, or org.madrid.common.<domain> for common schemas}}
            type: record
            name: OrderValue
            namespace: 'org.madrid.pédro.core.orders'
            fields:
              - name: id
                type: string
