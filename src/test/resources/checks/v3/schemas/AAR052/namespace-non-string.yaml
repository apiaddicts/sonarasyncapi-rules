asyncapi: 3.0.0
info:
  title: Avro Namespace Non String V3
  version: 1.0.0
channels:
  orders:
    address: orders
    messages:
      OrderNumber:
        payload:
          schemaFormat: 'application/vnd.apache.avro;version=1.9.0'
          schema: # Noncompliant {{AAR052: Avro namespace must follow the corporate pattern: org.madrid.<cod_poaps>.<classification>.<domain> for application schemas, or org.madrid.common.<domain> for common schemas}}
            type: record
            name: OrderValue
            namespace: 123
            fields:
              - name: id
                type: string
      OrderBoolean:
        payload:
          schemaFormat: 'application/vnd.apache.avro;version=1.9.0'
          schema: # Noncompliant {{AAR052: Avro namespace must follow the corporate pattern: org.madrid.<cod_poaps>.<classification>.<domain> for application schemas, or org.madrid.common.<domain> for common schemas}}
            type: record
            name: OrderValue
            namespace: true
            fields:
              - name: id
                type: string
      OrderObject:
        payload:
          schemaFormat: 'application/vnd.apache.avro;version=1.9.0'
          schema: # Noncompliant {{AAR052: Avro namespace must follow the corporate pattern: org.madrid.<cod_poaps>.<classification>.<domain> for application schemas, or org.madrid.common.<domain> for common schemas}}
            type: record
            name: OrderValue
            namespace:
              nested: value
            fields:
              - name: id
                type: string
      OrderArray:
        payload:
          schemaFormat: 'application/vnd.apache.avro;version=1.9.0'
          schema: # Noncompliant {{AAR052: Avro namespace must follow the corporate pattern: org.madrid.<cod_poaps>.<classification>.<domain> for application schemas, or org.madrid.common.<domain> for common schemas}}
            type: record
            name: OrderValue
            namespace:
              - org.madrid.p001.core.orders
            fields:
              - name: id
                type: string
