asyncapi: 3.0.0
info:
  title: Components Channel
  version: 1.0.0
servers:
  - host: api.example.com
    protocol: wss
channels:
  orders:
    $ref: '#/components/channels/orders'
components:
  channels:
    orders:
      address: orders
      messages:
        Order: # Noncompliant {{AAR024: Each message must declare a 'contentType' unless it is an Avro message}}
          payload:
            type: object
