asyncapi: 3.0.0
info:
  title: Empty Operation Key
  version: 1.0.0
channels:
  messages:
    address: messages
    messages:
      Message:
        payload: {type: object}
operations:
  "": # Noncompliant {{AAR051: The operationId must be present and follow camelCase naming convention}}
    action: send
    channel:
      $ref: '#/channels/messages'
