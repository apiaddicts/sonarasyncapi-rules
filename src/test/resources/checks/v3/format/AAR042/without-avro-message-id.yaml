asyncapi: 3.0.0
info:
  title: Message Identifier Avro V3 Missing
  version: 1.0.0
servers:
  - host: api.example.com
    protocol: kafka
channels:
  carga:
    address: carga
    messages:
      CargaMessage: # Noncompliant {{AAR042: Message should have a 'messageId' field for unique identification.}}
        payload:
          schemaFormat: 'application/vnd.apache.avro;version=1.9.0'
          schema:
            type: record
            name: CargaValue
            namespace: com.example.avro
            fields:
              - name: id
                type: string
