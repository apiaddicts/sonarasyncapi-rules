asyncapi: 3.0.0
info:
  title: No Operation IDs
  version: 1.0.0
servers:
  - host: api.example.com
    protocol: wss
channels:
  messages:
    address: messages
    messages:
      Message:
        payload: {type: object}
operations:
  SendMessage: # Noncompliant {{AAR012: Operation must declare an 'operationId'.}}
    action: send
    channel: messages
