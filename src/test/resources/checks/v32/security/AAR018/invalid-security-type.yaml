# Noncompliant {{AAR018: Invalid security type}}
asyncapi: 3.2.0
info:
  title: Invalid Security
  version: 1.0.0
servers:
  - host: api.example.com
    protocol: wss
components:
  securitySchemes:
    badAuth:
      type: invalid-type
channels:
  events:
    address: events
    messages:
      Event:
        payload: {type: object}
