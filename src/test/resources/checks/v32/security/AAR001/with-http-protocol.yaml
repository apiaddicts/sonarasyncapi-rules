# Noncompliant {{AAR001: Servers should use wss protocol}}
asyncapi: 3.2.0
info:
  title: AsyncAPI Example
  version: '1.0.0'
servers:
  - host: api.example.com
    protocol: ws
    pathname: /ws
channels:
  user-signup:
    address: user/signup
    messages:
      UserSignedUp:
        payload:
          type: object
