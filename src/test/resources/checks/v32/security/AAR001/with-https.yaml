asyncapi: 3.2.0
info:
  title: HTTPS API
  version: '1.0.0'
servers:
  - host: api.example.com
    protocol: https
    pathname: /api
channels:
  user-events:
    address: user/events
    messages:
      UserEvent:
        payload:
          type: object
