asyncapi: 3.2.0
info:
  title: Multi Server
  version: 1.0.0
servers:
  - host: api1.example.com
    protocol: wss
  - host: api2.example.com
    protocol: wss
channels:
  events:
    address: events
    messages:
      Event:
        payload: {type: object}
