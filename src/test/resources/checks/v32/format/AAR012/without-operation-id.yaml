# Noncompliant {{AAR012: Operation ID required}}
asyncapi: 3.2.0
info:
  title: No Operation IDs
  version: 1.0.0
servers:
  - host: api.example.com
    protocol: wss
channels:
  messages:
    address: messages
    messages:
      Message:
        payload: {type: object}
operations:
  SendMessage:
    action: send
    channel: messages
