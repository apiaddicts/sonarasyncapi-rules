asyncapi: 3.1.0
info:
  title: No Summaries
  version: 1.0.0
servers:
  - host: api.example.com
    protocol: wss
channels:
  events:
    address: events
    messages:
      Event:
        payload: {type: object}
operations:
  SendEvent: # Noncompliant {{AAR021: Provide a summary for each operation}}
    action: send
    channel: events
