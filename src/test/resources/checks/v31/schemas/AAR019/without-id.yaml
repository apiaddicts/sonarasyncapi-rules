asyncapi: 3.1.0
info:
  title: No ID
  version: 1.0.0
servers:
  - host: api.example.com
    protocol: wss
channels:
  users:
    address: users
    messages:
      User:
        payload: # Noncompliant {{AAR019: The AsyncAPI document should define an 'id' field.}}
          type: object
          properties:
            name:
              type: string
