# Noncompliant {{AAR024: Message validation required}}
asyncapi: 3.1.0
info:
  title: No Validation
  version: 1.0.0
servers:
  - host: api.example.com
    protocol: wss
channels:
  orders:
    address: orders
    messages:
      Order:
        payload:
          type: object
