asyncapi: 3.1.0
info:
  title: Undocumented Tags
  version: 1.0.0
servers:
  - host: api.example.com
    protocol: wss
tags:
  - name: undocumented # Noncompliant {{AAR010: Tags should be documented}}
channels:
  events:
    address: events
    messages:
      Event:
        payload: {type: object}
operations:
  PublishEvent:
    action: send
    channel: events
