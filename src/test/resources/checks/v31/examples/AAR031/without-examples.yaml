asyncapi: 3.1.0
info:
  title: No Examples
  version: 1.0.0
servers:
  - host: api.example.com
    protocol: wss
channels:
  users:
    address: users
    messages:
      User: # Noncompliant {{AAR031: All examples in message object should follow payload and headers schemas}}
        payload:
          type: object
          properties:
            id:
              type: string
            name:
              type: string
